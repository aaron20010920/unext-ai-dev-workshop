# 把這個檔複製成 .env.local（只在你自己電腦上跑的時候需要）
#
# 線上版（Vercel）不是改這裡 —— 是去 Vercel 專案的
# Settings → Environment Variables 加同名的 GROQ_API_KEY
#
# key 去哪拿：https://console.groq.com → API Keys → Create（免費，不用信用卡）

GROQ_API_KEY=你的_key_貼在這裡
